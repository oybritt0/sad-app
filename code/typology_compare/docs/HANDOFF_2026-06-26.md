# SAD Tool: Handoff / Restart Note - 2026-06-26 (morning)

Continues SAD_restart_2026-06-25_ui.md (the current frontier) and
SAD_restart_2026-06-25_structures.md (NSI build detail). Read this one first;
it carries today's plan plus the things most likely to bite.

================================================================
## WHERE WE ARE (one screen)

Retrieval tool: draw a boundary, get which of 37 North American stadium-anchored
districts it most resembles and why. Generation is a confirmed dead end at N=37.
Districts are read through the firm's four-typology framework (Entertainment,
Community, Innovation/Employment, Sports Park / Sports Tourism) on two axes:
local-to-visitor and civic-to-commercial.

Three channels are LIVE on the match server, port 8000:
- Demographic - Census ACS, 35 districts. POST /analyze
- Program     - Overture POIs, 39 districts. POST /analyze_program
- Structure   - FEMA/USACE NSI by floor area, 37 districts. POST /analyze_structures

Server boot prints all three (program 39, demographic 35, structure 37).
NSI verdict stands: own channel, not fused into program. It separates typologies
slightly better (0.0543 vs 0.0397) and recovers residential POI missed in 16 of
35, but is sport-blind (HAZUS has no sport class, anchor lands in retail) and
under-reads stories. The program-vs-structure disagreement is the signal.

Last session shipped the NSI channel end to end plus four UI fixes (anchor-checked
patchers, node-validated, backups taken): typology hover explainer, honest
program donut that is program_real-READY, closeness fallback for drawn districts,
drawn-district city label.

CONFIRMED by eye: typology fit chart both places; donut honest subtitle; closeness
fallback bars + label; three channels boot.
NOT yet confirmed by eye: typology hover tooltip; city label on a fresh draw.

================================================================
## TODAY, IN ORDER

1. Real NSI floor-area into the Program mix donut.
   The dashboard reads compare_manifest_geo.json. build_compare_manifest.py writes
   compare_manifest.json, a DIFFERENT file. A separate geo-manifest builder makes
   the _geo version. Find that builder first, then add per-district
   program_real = NSI shares_sqft (8 buckets), rerun it, donut auto-flips to
   "BY AREA". cProgram is already wired; no JS change needed.

2. Confirm the two unconfirmed fixes. If the city label is blank on a fresh draw,
   the work network is blocking Nominatim (same block that kills overpass-api.de).
   Fallback: server-side US Census geocoder in /analyze. Census IS reachable from
   the work network.

3. Structure channel's own "Most similar by structure" panel (POST
   /analyze_structures) in both the Map draw panel and the Compare dashboard,
   mirroring the program wiring. Same typology_fit contract.

4. Anchor-to-sport reclass v2: over the nsi_structures files, use
   anchor_overrides.json to reclassify the structure nearest each anchor into the
   sport bucket, de-inflating retail. Re-run structure_vs_program_eval for the lift.

================================================================
## POTENTIAL ISSUES (the things most likely to cost a session)

A. THE GEO-BUILDER IS NOT IN THE PROJECT FILES. Confirmed: nothing in the uploaded
   project knowledge writes compare_manifest_geo.json. build_compare_manifest.py
   writes the plain compare_manifest.json. The geo-builder lives only on the
   machine. ACTION before anything else: search code\ for whatever writes the _geo
   file (grep the folder for "compare_manifest_geo" or "_geo.json"). Until that
   script is in hand, step 1 cannot start. Upload it when found.

B. BUCKET KEYS CONFIRMED MATCHING (was a risk, now cleared). nsi_corpus_enrich.py
   defines ROSSETTI_BUCKETS = [sport, residential, hotel, retail_food_entertainment,
   office, parking, open_space, other] and shares_sqft is keyed by exactly those
   eight, which are the AMEN_C buckets cProgram expects. program_real can be injected
   straight from each district's NSI shares_sqft with NO remap. Just confirm the
   builder copies the dict as-is and does not lowercase or reorder.

C. STEP 1 AND STEP 4 INTERACT. NSI is sport-blind right now, so program_real will
   show sport near 0 and retail inflated (Detroit 81%, Arlington 95%). If the
   "by area" donut ships before the anchor-to-sport reclass (step 4), it is honest
   but reads misleading on exactly the districts the tool is about (the anchor
   reads as retail). Either do step 4 first, or ship step 1 with the donut subtitle
   still flagging the sport blind spot, then re-run after reclass.

D. CANADIAN DISTRICTS HAVE NO NSI. 4 are stubbed (available:false, NSI is US only).
   program_real will be missing for them. cProgram must fall back to POI counts
   per-district when program_real is absent. The "when present" guard exists;
   confirm it actually no-ops cleanly for those 4 rather than rendering "BY AREA"
   with empty data.

E. DASHBOARD STILL NAMES A DRAWN DISTRICT FROM THE FOLDER. extract(m) in
   compare_dash builds name/city from folder-name parts (parts[1], parts[2]), so a
   saved drawn district reads "Drawn district / Drawn boundary" regardless of the
   Map-side city label. The label only fixes the Map panel. The dashboard fixes
   itself only after a city-named district is saved (save sends name = resolved
   city) AND the manifest is rebuilt. Do not chase this as a JS bug.

F. PATCHING THE SERVER BLIND IS DANGEROUS. The sad_match_server.py in the project
   files may be stale relative to the machine (the structure channel was wired in
   last session). For step 2's Census geocoder, get the CURRENT server off the
   machine and patch that, anchor-checked. Backup exists: sad_match_server_20260625_1129.bak.py.

G. NAMING UNRESOLVED. Tool says "Sports Park", PDF says "Sports Tourism Districts".
   Same category. Pick one for UI text before adding the structure panel so the new
   strip is consistent.

================================================================
## PATHS / CONVENTIONS (unchanged)

- Scripts: C:\Users\jmeyers\Desktop\sad-app\code\
- Data:    C:\Users\jmeyers\Desktop\Detroit_Test\data
- UI:      C:\Users\jmeyers\Desktop\Detroit_Test\data\_compare_ui\
- QGIS bundled Python only: $bat = "C:\Program Files\QGIS 3.40.11\bin\python-qgis-ltr.bat"
- Detroit is folder 32; test it first. Folders 02-38.
- Typology colors: Entertainment #D85A30, Innovation #534AB7, Sports Park #1D9E75,
  Community #888780.
- Deliver edits as anchor-checked Python patchers (dry-run default, backup, verify
  every anchor exists, idempotent). Validate JS with node --check. New-named files,
  never overwrite. Read the real file before assuming. No em dashes.

================================================================
## FILES TO UPLOAD FOR TODAY

EOD zip (SAD_handoff_2026-06-25_EOD.zip) is reviewed and in hand: it holds the two
restart notes, both UI patchers, and the four structure-channel scripts. Still
needed for today:

- The geo-manifest builder (whatever writes compare_manifest_geo.json) - REQUIRED
  for step 1. NOT in the EOD zip; lives only on the machine. grep code\ for it.
- Current sad_match_server.py off the machine - for step 2 if the geocoder fallback
  is needed (the wired-in structure channel makes any older copy stale).
- One newest derived/structures/nsi_structures_*.json (Detroit) - only to eyeball
  the shares_sqft values; the keys are already confirmed correct.
