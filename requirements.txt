# Core web
Flask==3.1.2
gunicorn==23.0.0
requests==2.32.5
# Geo stack (GDAL provided by the base image; let pip pick GDAL-compatible
# numpy/fiona/rasterio rather than hard-pinning versions that could clash with
# the base image's compiled bindings).
geopandas>=1.0,<1.2
shapely>=2.0,<2.2
fiona>=1.9,<1.11
pyproj>=3.6
rasterio>=1.3,<1.5
osmnx>=2.0,<2.1
duckdb>=1.0
networkx>=3.2
# Numerics — keep numpy <2.3 to stay compatible with the geo wheels and the
# base image's GDAL python bindings.
numpy<2.3
pandas>=2.2
scipy>=1.13
scikit-learn>=1.4
scikit-image>=0.24
# Raster/STAC (M22)
pystac>=1.10
pystac-client>=0.8
planetary-computer>=1.0
# Plotting / imaging
matplotlib>=3.8
Pillow>=10.0
pygris
